# pydantic-versions

`pydantic-versions` is an early-stage package for exploring versioned Pydantic schemas.

The package is intentionally minimal while the design direction is still being researched.
